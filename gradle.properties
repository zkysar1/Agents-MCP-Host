# Gradle properties for Agents-MCP-Host

# JVM settings (increased memory for stability)
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+UseG1GC

# Gradle daemon settings - DISABLED for WSL2 stability
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true

# WSL2-specific settings to prevent file locking issues
org.gradle.vfs.watch=false
org.gradle.configureondemand=true
org.gradle.console=plain

# Increase timeout for long-running operations
org.gradle.internal.http.connectionTimeout=60000
org.gradle.internal.http.socketTimeout=60000

# Java toolchain settings
org.gradle.java.installations.auto-download=false
org.gradle.java.installations.auto-detect=true

# Project-specific settings
systemProp.file.encoding=UTF-8
systemProp.java.awt.headless=true
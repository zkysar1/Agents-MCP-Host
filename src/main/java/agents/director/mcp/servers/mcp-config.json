{
  "mcpServers": {
    "httpServers": {
      "calculator": {
        "enabled": false,
        "port": 8086,
        "description": "Mathematical operations server"
      },
      "weather": {
        "enabled": false,
        "port": 8087,
        "description": "Weather information server"
      },
      "database": {
        "enabled": false,
        "port": 8088,
        "description": "Database operations server with sampling support"
      },
      "filesystem": {
        "enabled": false,
        "port": 8089,
        "description": "File system operations with roots/resources"
      },
      "oracle": {
        "enabled": true,
        "port": 8086,
        "description": "Unified Oracle MCP server with 24 comprehensive tools for database operations, metadata, and orchestration"
      }
    },
    "localServers": {
      "example-stdio-server": {
        "enabled": false,
        "command": "node",
        "args": ["path/to/mcp-server.js"],
        "environment": {
          "NODE_ENV": "production"
        },
        "description": "Example local MCP server via stdio"
      },
      "python-mcp-server": {
        "enabled": false,
        "command": "python",
        "args": ["-m", "mcp_server"],
        "environment": {
          "PYTHONPATH": "./lib"
        },
        "description": "Example Python MCP server"
      }
    }
  },
  "clientConfigurations": {
    "dual": {
      "enabled": false,
      "connects": ["calculator", "weather"],
      "description": "Connects to Calculator and Weather servers"
    },
    "single-db": {
      "enabled": false,
      "connects": ["database"],
      "description": "Connects only to Database server"
    },
    "filesystem": {
      "enabled": false,
      "connects": ["filesystem"],
      "description": "Connects to FileSystem server with permissions"
    },
    "oracle": {
      "enabled": true,
      "connects": ["oracle"],
      "description": "MCP client for Oracle server - connects via HTTP to port 8086"
    },
    "local": {
      "enabled": false,
      "type": "stdio",
      "description": "Manages local process-based MCP servers"
    }
  },
  "toolNaming": {
    "usePrefixing": true,
    "prefixSeparator": "__",
    "supportUnprefixed": true
  },
  "transport": {
    "httpTimeout": 30000,
    "sseReconnectDelay": 5000,
    "stdioBufferSize": 8192
  }
}
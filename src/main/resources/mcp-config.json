{
  "mcpServers": {
    "httpServers": {
      "calculator": {
        "enabled": false,
        "port": 8086,
        "description": "Mathematical operations server"
      },
      "weather": {
        "enabled": false,
        "port": 8087,
        "description": "Weather information server"
      },
      "database": {
        "enabled": false,
        "port": 8088,
        "description": "Database operations server with sampling support"
      },
      "filesystem": {
        "enabled": false,
        "port": 8089,
        "description": "File system operations with roots/resources"
      },
      "oracle_metadata": {
        "enabled": true,
        "port": 8081,
        "description": "Oracle metadata and structure navigation"
      },
      "oracle": {
        "enabled": true,
        "port": 8085,
        "description": "Oracle database operations with metadata and query tools"
      },
      "oracle-tools": {
        "enabled": true,
        "port": 8086,
        "description": "Oracle tools exposed as individual MCP capabilities for orchestration"
      }
    },
    "localServers": {
      "example-stdio-server": {
        "enabled": false,
        "command": "node",
        "args": ["path/to/mcp-server.js"],
        "environment": {
          "NODE_ENV": "production"
        },
        "description": "Example local MCP server via stdio"
      },
      "python-mcp-server": {
        "enabled": false,
        "command": "python",
        "args": ["-m", "mcp_server"],
        "environment": {
          "PYTHONPATH": "./lib"
        },
        "description": "Example Python MCP server"
      }
    }
  },
  "clientConfigurations": {
    "dual": {
      "enabled": false,
      "connects": ["calculator", "weather"],
      "description": "Connects to Calculator and Weather servers"
    },
    "single-db": {
      "enabled": false,
      "connects": ["database"],
      "description": "Connects only to Database server"
    },
    "filesystem": {
      "enabled": false,
      "connects": ["filesystem"],
      "description": "Connects to FileSystem server with permissions"
    },
    "oracle": {
      "enabled": true,
      "connects": ["oracle", "oracle_metadata"],
      "description": "Connects to Oracle servers"
    },
    "oracle-tools": {
      "enabled": true,
      "connects": ["oracle-tools"],
      "description": "Client bridge for oracle-tools server exposing 11 capabilities"
    },
    "local": {
      "enabled": false,
      "type": "stdio",
      "description": "Manages local process-based MCP servers"
    }
  },
  "toolNaming": {
    "usePrefixing": true,
    "prefixSeparator": "__",
    "supportUnprefixed": true
  },
  "transport": {
    "httpTimeout": 30000,
    "sseReconnectDelay": 5000,
    "stdioBufferSize": 8192
  }
}